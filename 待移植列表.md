# 上游能力待移植列表

对比基线：

- 上游：`coolci/OVH_WEBUI`，提交 `0fecbf39548fb48d55a5ad6874d30b3c2a512b07`（`1.2.3`）
- 本地：`e806e4823dc511885aee60cc03c1ddc50eb7ad9a`（`feat: add reliability and credential protection`）
- 本文是本轮移植的执行清单；每一项只有在代码、测试和适用的前端构建均通过后才标记为“已完成”。

状态说明：`待处理`、`进行中`、`已完成`、`部分完成/需后续`、`有意不移植`。

## 一、确认未移植且优先处理

### 1. SPLA 流程被固定许可证值替代

- 状态：**已完成**
- 现状：本地 `src/components/server-control/PowerTab.tsx` 直接提交固定的公开 KMS 序列号；上游 `SplaDialog` 支持授权类型选择、用户输入序列号、空值校验和风险提示。
- 移植范围：新增可编辑 SPLA 对话框，支持 `os`、`sqlstd`、`sqlweb`，只提交用户明确输入的序列号；移除固定密钥。
- 验收：空序列号不能提交；请求体包含用户选择的 `type` 和 `serialNumber`；后端路由保持兼容；前端类型检查和构建通过。

### 2. 到期终止策略

- 状态：**已完成**
- 现状：本地只有立即终止确认和续费字段更新；缺少安全区分“到期终止”和“立即终止”的策略接口。
- 移植范围：为独服和 VPS 增加 `PUT /termination-policy`，读取生命周期状态并安全更新 `terminationPolicy`；前端增加策略读取、选择和提交入口。
- 验收：到期终止不调用立即终止接口；合同/不可修改状态得到明确错误；独服和 VPS 均有后端测试。

### 3. 公共请求共享代理

- 状态：**已完成**
- 现状：账户级 OVH client 已隔离代理，但 catalog、实时库存、VPS 目录/可用性等公共请求仍使用裸 `http.Client`。
- 移植范围：增加共享公共 client，按默认账户代理配置；公共请求统一经共享 transport；代理变更时失效旧连接；无代理时明确直连。
- 验收：公共请求使用配置代理；代理 URL 不泄漏凭据；代理失败可被现有健康机制识别；并发读写通过 race 测试。

### 4. VPS 动态目录与 VPS 自动下单

- 状态：**已完成**
- 现状：本地前后端硬编码 `vps-2025-model1..6`，只有库存监控，没有动态型号目录和购买流程。
- 移植范围：移植按子公司查询的 VPS 目录、成功/失败缓存和旧值回退；新增模型接口；移植 VPS 购物车、数量、区域、系统选项和可选支付流程；前端改为动态模型。
- 已完成：新增按子公司动态目录接口与前端查询，按 `order-funnel:show` 过滤并保留最近成功目录；VPS 订阅新增 `autoOrder`、`quantity`、`os`、`autoOrderAccountId` 持久化字段；补货状态从无货转有货后先走既有库存通知，再使用指定账户创建 VPS 购物车/机房/系统配置订单；自动付款继续拒绝并固定关闭；账户与子公司跨站点组合在保存和下单两层拒绝；失败购物车清理，结账请求发出后保留不确定购物车以避免重复下单。
- 验收：停售硬编码型号不再强制显示；目录失败可使用最近成功缓存；跨子公司不可下单；自动下单不会绕过账户/代理校验；覆盖目录配置、跨区域校验、VPS 字段 round-trip 与采购配置测试。

### 5. 目录 verdict、区域缓存/singleflight 与 Eco 安全匹配

- 状态：**已完成**
- 现状：本地目录解析存在，但没有统一 `OK/Unknown/CrossRegion/NotEco/NoSuchPlan` 判定；区域探测和 Eco addon 匹配较弱。
- 移植范围：新增统一 verdict；按子公司和 planCode 做 TTL/负缓存及 singleflight；移植顺序无关、歧义安全的 `MatchEcoOption`，采购和询价共用。
- 已完成：新增 `PlanVerdict`/`ClassifyPlan`，区分目录故障、跨区、非 Eco 和三站点均无记录；新增公开目录按子公司的 2 小时缓存、30 秒失败缓存、过期成功值回退和 singleflight；新增 `(planCode, region)` 可用性探测缓存与失败静默；统一 Eco 四档匹配并接入监控配置解析、询价和采购；区域值改为目录权威值，保留静态安全兜底；队列和快速下单在可证明的终态前置拒绝；移除会产出 `usa`/`apac` 的旧区域 owner 和旧目录查找测试依赖；启动时后台预热账户子公司目录；catalog handler 复用唯一站点归属表。
- 验收：跨区域、未知、非 Eco、无库存可区分；重复并发探测只产生一次上游请求；前缀相似 addon 不会选错；覆盖顺序扰动和容量边界测试。

### 6. 完整 proxyguard 动作

- 状态：**已完成**
- 现状：本地只记录内存熔断状态并门禁自动动作，不持久化暂停队列、不关闭订阅自动下单、不通知恢复。
- 移植范围：增加失败窗口/通知冷却；暂停该账户 `running/pending` 队列并落库；关闭对应订阅自动下单但保留订阅；恢复和触发均进入通知 outbox；提供手动恢复语义。
- 已完成：代理 transport 错误统一包装为 `ProxyError`，普通 OVH API 错误不计入熔断；按账户在 2 分钟窗口内连续 3 次失败触发，30 分钟冷却后可发送仍不可用提醒；触发时持久化暂停该账户 `running/pending` 队列，并关闭独服/VPS 订阅自动下单，同时保留订阅、通知设置和自动下单账户；恢复时只恢复带 proxyguard 标记的队列和订阅，用户原本暂停或关闭的项目不受影响，人工编辑会清除对应标记；重启从持久化标记恢复账户门禁；触发、提醒、恢复都写入现有持久化通知 outbox，渠道失败不回滚核心状态；代理 URL 和错误文本在 transport、action、outbox 构造及分发边界多层脱敏；人工 reset 复用同一恢复 owner，并自动恢复本次熔断标记的队列和订阅。
- 验收：任务和订阅不会被删除；重启后门禁与暂停标记保留；自动下单被关闭而补货通知保留；代理恢复后自动恢复本次熔断暂停的队列和自动下单，且不覆盖用户原有暂停/关闭状态；通知失败不阻塞核心状态变更。

### 7. 未知货币不能默认显示 EUR

- 状态：**已完成**
- 现状：目录缺失或 `currencyCode` 缺失时，前端多处回退 `EUR`。
- 移植范围：引入明确的未知货币表示和安全格式化；只有已知子公司/币种才显示符号，未知时保留代码或显示“币种未知”。
- 已完成：移除前端目录索引、后端购物车询价、目录拆价和订单详情补全中的 EUR/USD 猜测；新增共享币种规范化和金额格式化，已知代码显示对应符号，未知代码保留代码，空值显示“币种未知”；历史订单、账户订单、服务器卡片/详情、合同价格、监控通知和 Telegram 价格文本均不再伪造默认币种；US/CA 等真实 API 币种继续按返回值保留。
- 验收：缺失目录或币种不会显示 EUR；US/CA 目录不会被误显示为 EUR；历史价格缺失币种显示“币种未知”；覆盖后端未知币种/规范化/通知格式化测试，并通过前端类型检查和构建。

## 二、部分移植或较窄实现

### 8. 订单状态刷新

- 状态：**已完成**
- 现状：本地已有后台订单状态轮询、事务写入和通知 outbox，但缺少手动刷新接口与前端操作。
- 移植范围：新增 `POST /api/purchase-history/refresh-status`，复用同一状态更新 owner；前端历史页增加刷新操作和进行中状态。
- 已完成：将后台轮询和手动刷新绑定到同一个 `OrderStatusLoop` 实例与互斥锁；手动刷新强制跳过单订单两分钟节流，但跳过终态及超过 30 天窗口的订单；HTTP 入口增加 15 秒限频、45 秒请求超时、可读的成功/部分失败/忙碌响应和失败明细；候选仍由数据库限定为成功且有 `order_id` 的历史；状态和通知继续通过 `UpdateHistoryOrderStatus` 与同一事务 outbox 原子落库；历史页增加“刷新订单状态”按钮及进行中状态，并在完成后重载历史。
- 验收：只刷新已有 `order_id` 的成功历史；不会重复并发轮询；状态和通知仍原子落库；失败返回可读结果。

### 9. 采购阶段耗时统计

- 状态：**已完成**
- 现状：本地没有 `PhaseTiming`、`TotalMs`、历史持久化、`GET /api/queue/timings` 或前端展示。
- 移植范围：在 cart、配置、checkout、支付/状态补全等阶段采集墙钟耗时；写入历史；提供汇总接口；前端显示阶段耗时。
- 已完成：新增线程安全的单次采购 timeline，覆盖查库存、创建/绑定购物车、基础商品、配置参数、硬件选项、checkout 和异步订单详情补全；成功、失败、待核实和取消路径保留已完成阶段；最近一轮按 `planCode@datacenter` 保存最多 500 条链路快照并由 `GET /api/queue/timings` 返回；history 增加 `timing` JSON 与 `total_ms` 列，启动时幂等补列，所有成功/失败/恢复写入路径统一持久化；历史页和队列页增加可点击耗时标签，旧历史与空数据保持兼容。
- 验收：失败和取消也能保留已完成阶段；统计不会改变下单结果；旧历史记录兼容；接口和 UI 有空数据状态。

### 10. 代理诊断接口与指纹配置

- 状态：**已完成**
- 现状：本地有代理 URL 校验、账户隔离、状态查询和 reset，但缺少出口 IP、目标延迟/连通性测试、profile 列表和完整代理检查 API。
- 移植范围：增加账户级代理测试、出口 IP、目标探测、全局状态和可用 fingerprint profile；前端账户页增加诊断操作。
- 已完成：复用 `internal/ovh` 账户代理 owner 增加 `default`、`chrome-like`、`firefox-like`、`legacy-http1` 白名单 profile，实际应用 TLS 版本/ALPN/套件集合/请求头；保留旧 `ua:` 值的兼容读取并给出迁移告警；新增 `GET /api/accounts/proxy-status`、`POST /api/accounts/:id/proxy-test`、`POST /api/accounts/:id/proxy-check`，按账户当前代理和 endpoint 测试出口 IP、OVH 控制台/库存目标连通性及多次采样延迟；诊断 client 不挂 proxyguard reporter，不改变队列、订阅或下单状态；所有响应和错误均脱敏；设置页增加 profile 选择、代理状态、出口 IP 与链路检测对话框。
- 验收：诊断请求使用目标账户代理；失败结果不触发业务下单；代理凭据永不返回；支持的 profile 有明确白名单和测试覆盖。

## 三、有意不移植

以下上游内容与本地产品/部署边界冲突，除非另有明确需求，不列入本轮执行：

- `backend/internal/updater/*`、`POST /api/version/update`、`GET /api/version/update/status`：运行中二进制自更新与单镜像 Docker 部署冲突。
- `Dockerfile.backend`、`Dockerfile.frontend`、Caddy 和拆分式 compose：本地保持单镜像部署。
- `BrandBar.tsx`、`AuthorWatermark.tsx`：品牌展示，不影响业务能力。
- Telegram `getUpdates` 长轮询：本地采用带 secret 的 Webhook，并扩展飞书/微信。
- `autoPay` 自动付款：本地当前明确固定关闭，作为资金安全边界保留；除非用户单独要求，不自动开放。

## 四、本地已有或更完整的能力

- 飞书 WebSocket 长连接、注册和交互卡片。
- 微信 iLink 登录、轮询和快速下单。
- 持久化通知 outbox、多渠道投递和失败重试。
- 实时库存、预添加服务器和下架套餐跟踪。
- `/reboot` 显示名称回退。
- 账户级代理隔离、无直连回退、代理凭据加密与旧明文迁移。
- checkout 不确定状态恢复、瞬时错误分类和后台订单状态轮询。
- 单镜像 Docker 部署。

## 执行记录

| 顺序 | 项目 | 状态 | 验证证据 |
|---:|---|---|---|
| 1 | SPLA 用户输入流程 | 已完成 | `npm run build`; `npx tsc --noEmit -p tsconfig.app.json` |
| 2 | 到期终止策略 | 已完成 | `go test ./internal/handlers ./internal/ovh`; `npx tsc --noEmit -p tsconfig.app.json` |
| 3 | 公共请求共享代理 | 已完成 | `go test -race ./internal/ovh ./internal/handlers ./internal/vps ./internal/app`; `go vet ./internal/ovh ./internal/handlers ./internal/vps ./internal/app` |
| 4 | VPS 动态目录与自动下单 | 已完成 | `go test ./... -count=1`; `go test -race ./internal/vps ./internal/db ./internal/handlers ./internal/ovh ./internal/app -count=1`; `go vet ./internal/vps ./internal/db ./internal/handlers ./internal/ovh ./internal/app`; `npx tsc --noEmit -p tsconfig.app.json`; `npm run build` |
| 5 | 目录 verdict/区域缓存/Eco 匹配 | 已完成 | `go test ./... -count=1`; `go test -race ./internal/catalog ./internal/handlers ./internal/price ./internal/purchase ./internal/ovh -count=1`; `go vet ./...`; `npx tsc --noEmit -p tsconfig.app.json`; `npm run build` |
| 6 | 完整 proxyguard 动作 | 已完成 | `go test ./... -count=1`; `go test -race ./internal/proxyguard ./internal/ovh ./internal/db ./internal/monitor ./internal/app ./internal/handlers ./internal/vps -count=1`; `go vet ./...` |
| 7 | 未知货币处理 | 已完成 | `go test ./... -count=1`; `go test -race ./internal/price ./internal/monitor ./internal/purchase ./internal/handlers ./internal/ovh ./internal/app -count=1`; `go vet ./...`; `npx tsc --noEmit -p tsconfig.app.json`; `npm run build` |
| 8 | 手动订单状态刷新 | 已完成 | `go test ./... -count=1`; `go test -race ./internal/purchase ./internal/handlers ./internal/app ./internal/monitor ./internal/db -count=1`; `go vet ./...`; `npx tsc --noEmit -p tsconfig.app.json`; `npm run build` |
| 9 | 采购阶段耗时统计 | 已完成 | `go test ./... -count=1`; `go test -race ./internal/purchase ./internal/db ./internal/handlers -count=1`; `go vet ./...`; `npx tsc --noEmit -p tsconfig.app.json`; `npm run build` |
| 10 | 代理诊断接口与指纹配置 | 已完成 | `go test ./... -count=1`; `go test -race ./internal/ovh ./internal/handlers ./internal/app ./internal/proxyguard -count=1`; `go vet ./...`; `npx tsc --noEmit -p tsconfig.app.json`; `npm run build` |
